{
	"name": "puppeteer-pdf-server",
	"version": "0.0.1",
	"description": "A simple server to generate PDFs using puppeteer",
	"scripts": {
		"start": "node index.js"
	},
	"dependencies": {
		"puppeteer": "^22.1.0"
	}
}
